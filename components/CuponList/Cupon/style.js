import { View as OView } from 'react-native';
import styled from 'styled-components/native';
import { ListItem as OListItem, Button as OButton, Text as OText, } from 'react-native-elements';

export const View = styled.OView``;
export const ListItem = styled.OListItem``;
export const Button = styled.OButton``;
export const Text = styled.Text``;
